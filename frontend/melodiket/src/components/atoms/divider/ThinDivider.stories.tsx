import type { Meta, StoryObj } from '@storybook/react';
import ThinDivider from './ThinDivider';

const meta: Meta<typeof ThinDivider> = {
  component: ThinDivider,
};

export default meta;
type Story = StoryObj<typeof ThinDivider>;

export const Default: Story = {};

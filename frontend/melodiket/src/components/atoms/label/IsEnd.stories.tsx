import type { StoryObj, Meta } from '@storybook/react';
import IsEnd from './IsEnd';

const meta: Meta<typeof IsEnd> = {
  component: IsEnd,
};

export default meta;

type Story = StoryObj<typeof IsEnd>;

export const Default: Story = {};
